import { defineComponent } from 'vue';

export default defineComponent({
    setup() {
        return () => (
            <>
                <div className="app-container">欢迎学习ts</div>
            </>
        );
    },
});
